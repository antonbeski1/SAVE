// src/app/page.tsx
// You can optionally import components if needed, but for a disabled panel,
// it might not be necessary.
// import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
// import { Input } from "@/components/ui/input";
// import { Label } from "@/components/ui/label";
// import { LifeBuoy } from "lucide-react";

export default function LoginPage() {
  return (
    <>
      {/* Login panel disabled for now */}
      <div className="flex min-h-screen w-full items-center justify-center bg-black text-white text-2xl">
        The login panel is currently disabled.
      </div>
    </>
  );
}
